extends ../layout

block content
    h1= title

    include error

    form(method='POST', action="/books/new")
        include form
        p
            input(type="submit" value="Create New Book")
        p
            a(class="button" href="/books/" + urlParams) Cancel